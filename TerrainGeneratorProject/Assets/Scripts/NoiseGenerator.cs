using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class NoiseGenerator
{
    public static float[,] GenerateNoiseMap(int noiseMapWidth, int noiseMapHeight, float noiseScale)
    {
        return new float[noiseMapWidth, noiseMapHeight];
    }
}
